/**
 * Configuration Index
 * Export all configuration modules for easy import
 */

export * from './supabase.config';

// Export default modules
import supabaseConfig from './supabase.config';

export { supabaseConfig };